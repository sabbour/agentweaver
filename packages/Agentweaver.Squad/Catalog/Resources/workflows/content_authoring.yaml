# Content Authoring Workflow (Feature 015 US3)
# Content creation pipeline: research → draft → editorial review → RAI check → publish → scribe.
# Includes an editorial review revise loop and a RAI safety check and human review before publishing.
# No code-merge semantics — publish is the terminal action for content delivery.

id: content-authoring
name: Content Authoring
description: Content creation pipeline with research, drafting, editorial review, RAI safety check, and publishing. Use for articles, documentation, long-form content, and any work where the output is published text rather than merged code.
version: "1.0"

start: research

nodes:
  - id: research
    type: prompt
    label: Research
    role: agent
    kind: live

  - id: draft
    type: prompt
    label: Draft
    role: agent
    kind: live

  - id: edit
    type: prompt
    label: Editorial Review
    role: review
    kind: live
    prompt: Review the drafted content editorially and prepare it for the RAI safety check.

  - id: rai-check
    type: check
    label: RAI Check
    role: review
    kind: gate
    gate_kind: rai
    branches:
      - revise
      - safety-failed
      - no-changes
      - review

  - id: human-review
    type: check
    label: Human Review
    role: review
    kind: gate
    gate_kind: human-review
    branches:
      - approved
      - request-changes
      - declined

  - id: publish
    type: prompt
    label: Publish
    role: agent
    kind: live
    prompt: Publish or package the approved content without invoking code merge semantics.

  - id: terminal-safety-failed
    type: terminal
    label: Safety Failed
    role: plumbing
    kind: terminal

  - id: terminal-declined
    type: terminal
    label: Declined
    role: plumbing
    kind: terminal

  - id: done
    type: terminal
    label: Done
    role: plumbing
    kind: terminal

edges:
  - from: research
    to: draft

  - from: draft
    to: edit

  # Editorial review flows to the RAI gate.
  - from: edit
    to: rai-check

  # RAI verdict routing.
  - from: rai-check
    to: draft
    when: revise

  - from: rai-check
    to: terminal-safety-failed
    when: safety-failed

  - from: rai-check
    to: done
    when: no-changes

  - from: rai-check
    to: human-review
    when: review

  - from: human-review
    to: publish
    when: approved

  - from: human-review
    to: draft
    when: request-changes

  - from: human-review
    to: terminal-declined
    when: declined

  - from: publish
    to: done
