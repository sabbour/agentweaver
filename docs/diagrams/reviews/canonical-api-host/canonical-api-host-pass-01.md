# Pass 1 - visual upgrade

Input: inspected A5 pitch PNG and source. Output: `canonical-api-host-pass-01.drawio` and matching Desktop PNG.

Expanded two conceptual anchors into eight evidenced components with tiered startup/request surfaces, group titles, native symbols, icon/title/subtitle/detail/metadata/badge hierarchy, 5 px accents, restrained shadows, explicit provider/storage boundaries, resource-role checks, and HTTP/data responses. The fail-closed classification note is `native:flowchart` (`note`). Native database cylinder and cloud complete the symbol inventory; no custom technology logos or embedded image bytes.

The first candidate measured 8.098173x and lacked group labels and response flow. The second measured 8.972053x and still omitted explicit failure behavior. Both candidates remain saved. These were redesign attempts within pass 1, not separate correction passes. Adding meaningful response relationships, group headings, a native database cap, and the classification failure annotation completed the upgrade. No padding, invisible objects, duplicated cells, or off-page geometry was introduced.

Final A5 metric, from `growth.json`:

- Metric: `visible-semantic-canonical-xml-v1`
- Baseline: 2881
- Result: 26129
- Ratio: 9.06942x
- Visible structures: 10 -> 89
- Invisible/off-page/duplicate/padding findings: none

Opened the actual A5 `-pass-01.png` enlarged and the 794 x 559 `-pass-01-print.png` proof. Main hierarchy and labels are legible; metadata is secondary. All nine arrowheads attach to intended card boundaries. Request, delegation and invocation routes are distinct from marigold response routes. No crossings or junctions are required, and no fake junction dots/masks exist.

Orientation defects: 0. Overlap defects: 0. Arrow defects: 0. Composition/content/style are frozen for passes 2-4. Source/relationship evidence remains in `content-model.json`; use correction-only passes thereafter.
