# Bounded remediation tooling observations

## ERR-20260913-inventory — global inventory validation during concurrent cleanup

- Area: docs.
- Status: scoped workaround; parent owns global reconciliation.
- The exact-name `inventory-diagrams.mjs --list` request validates the entire
  inventory before applying its selector. It failed on unrelated orphaned entries
  being consolidated by the parent.
- No inventory, reconciliation, plan, or unrelated asset was changed to bypass the
  failure. Stable paths remain the nine assigned source/PNG/hash families.
- Use the renderer's repeated exact `--spec` arguments, without an area or
  disposition selector, for the authorized selective render and drift check.
- The pitch skill is present in the target worktree but not registered in the
  session's skill list. Its SKILL, checklist, schema, and iteration helper files
  were read directly from that worktree.

## Corrected export findings

- Pass 1: opaque side-label backgrounds covered unrelated vertical routes.
  Pass 2 places label bounds before the independently packed rail bounds.
- Pass 2: selection's `2 unusable` label wrapped across its own connector.
  Pass 3 explicitly uses two lines with sufficient height above the connector.
- The fallback's small native process glyph was enlarged inside its existing card
  in pass 2. No workflow node, branch, or execution behavior was changed.
