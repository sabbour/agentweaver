# Pitch

Read `research-and-contract.md` for the three independent GPT-6 Astra findings,
node/relationship evidence, asset credits, stable paths and unresolved inventory claim.

One A5 landscape page; uncompressed editable XML derived from the Fluent template after
loading the reusable library. Three overview responsibility cards are a new abstraction,
not a transcription or minimization of the legacy diagram: planning, dispatch/child
execution, collective assembly. Main reading direction is left to right.

Exported by the actual pinned draw.io Desktop 31.4.5 with repository PNG recipe:
`--export --format png --border 16 --scale 2`.
Opened the actual 1611 x 964 PNG and its 0.48-scale 96-DPI print-size inspection copy.
No clipped text, reverse arrow, collision or off-page object observed. Both arrows point
from planning through execution to collective assembly; labels remain legible.

Known incompleteness handed to docs-diagram-iterate: missing ingress, state ownership,
dispatch recovery, separate child responsibility, actual native symbols, complete
card accent/icon/metadata/badge hierarchy, grouping, and semantic review-return rail.
The three-card overview intentionally does not claim publication completeness.

The next stage is the visual-upgrade pass, followed by correction-only passes 2-4+.
Pitch baseline: 3,162 canonical visible-semantic units, 8 visible structures.
