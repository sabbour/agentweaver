# Pass 02 — corrections only

Input: inspected pass-01 PNG. Saved a new source and exported a new PNG; no pitch/pass source was overwritten.

## Corrections

- Moved only the `e06` repair waypoints into the gutter beneath the generator; it no longer crosses the prompt-context input. It remains the sole dashed marigold return and still enters the same generator.
- Moved the persistence-boundary title to right alignment and the `e08` explicit-Save label upstream; the arrow no longer pierces the group title.
- Increased error-card/footer geometry to separate existing metadata from badges. Expanded the enclosing lower group inside the same A5 page. No text, native symbol, responsibility, relationship or visual language was added/removed.

## Actual PNG review

Opened `canonical-workflow-authoring-pass-02-print.png` and the actual `canonical-workflow-authoring-pass-02.png` (2190 × 3151).

- Print: the success spine and failures remain readable. The context and repair flows are now independent and visibly distinct.
- Enlarged: repair rail and Save heading are clean. Registry row is contained within the page. The reload-error metadata still wraps close to the right-hand badge; constrain its existing text box to the space left of the badge.
- Resizing the second-invalid error card shifted its center: `e07` retains an old target-adjacent waypoint, creating a small unwanted elbow at the arrowhead. Its label also sits too near the error-card corner. Align the waypoint to the new target center and move the label earlier on the same connector.
- **Remaining counts:** orientation 0; overlap 1; arrow 1.

Pass 3 will make only those two local geometry corrections; all source-backed content remains fixed.
