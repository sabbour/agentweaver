# Pass 01 — visual upgrade

Input: the separately exported and inspected pitch; output: the separately preserved pass-01 source and PNG. This is the only redesign pass.

## Meaningful growth

`visible-semantic-canonical-xml-v1`: **3,882 → 36,306**, **9.352396×**, 12 → 117 visible structures. The checker reports no invisible, off-page, duplicate or metadata-padding objects. An initial pre-export checker rejected negative edge-label offsets as off-page points; those offsets were corrected before this pass's sole PNG export. No whitespace, metadata or embedded image data contributes to the gate.

The pitch already had native process/document notation, title/subtitle/monospace metadata/pill hierarchy, semantic accents, warm card shadows and a tiered boundary. It was not stripped to manufacture a baseline.

## Meaningful additions

Expanded the two grounded abstractions into seven success-path responsibilities, five distinct context/error responsibilities, the single repair action, and thirteen explicit relationships:

- Ownership/provider admission and prompt context before the actual generator.
- Separate candidate validation with the loader **and** binder.
- Exactly one semantic correction return and a distinct second-invalid error.
- Unsaved human review/edit versus the explicit PUT Save.
- Save revalidation, contained file write and registry reload; separate failure outcomes before and after writing.
- Independently editable native icons, text hierarchy and semantic badges in each card.

No deployment boundary, automatic execution, new provider behavior or additional retry was invented. No external asset was added. Native classification: process/decision/document/terminator = `native:flowchart`; generator and registry component icons = `native:uml` (`component` built-in shape); all card/group/badge chrome = `custom:agentweaver`.

## Actual PNG review

Opened both `canonical-workflow-authoring-pass-01-print.png` and the actual 2190 × 3111 `canonical-workflow-authoring-pass-01.png`.

- **Orientation:** clean top-to-bottom success spine; right-side failures and a marigold repair return. No reversed relationship.
- **Print:** the expansion is dense but the success spine, native icons and distinction between unsaved generation and Save are readable.
- **Enlarged overlaps:** explicit-save connector intersects the save-boundary title; second-invalid metadata touches its badge; reload-error metadata wraps into neighboring text/badge.
- **Routing:** the repair rail crosses the prompt-context connector beside its label, making the two relationships harder to distinguish. Correct by routing the repair through the lower gutter.
- **Remaining counts:** orientation 0; overlap 3; arrow/routing 1.

Pass 2 will correct only these collisions and routing defects. Content, visual language and composition are now fixed.
