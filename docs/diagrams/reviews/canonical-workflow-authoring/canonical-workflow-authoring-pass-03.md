# Pass 03 — corrections only

Input: the independently exported and inspected pass-02 PNG.

## Corrections

- Limited `reload-error-meta` to the existing left-of-badge space. The unchanged wording wraps to two readable lines without touching the pill.
- Aligned `e07`'s target-adjacent waypoint with the resized error card's actual center. The arrow now enters horizontally from the left, rather than forming a tiny false turn at the endpoint.
- Moved the same connector's “invalid again” label earlier on its route into the open gutter.

No content, node, edge, native notation or visual language changed.

## Actual PNG review

Opened both `canonical-workflow-authoring-pass-03-print.png` and the actual 2190 × 3151 `canonical-workflow-authoring-pass-03.png`.

- Print: generation remains distinct from Save; the lower metadata and all failure conditions are readable.
- Enlarged: second-invalid arrow ends on the intended error card with a horizontal arrowhead; the two invalid paths have distinct source ports and labels; reload-error text clears its badge.
- Neighbor check: prompt-context, generator/repair, validation/review and write/registry neighbors remain collision-free. The explicit Save crosses the group boundary through clear space rather than text.
- **Remaining counts:** orientation 0; overlap 0; arrow 0.

Pass 4 will export a distinct no-change source/PNG and trace every arrow. The four-pass minimum is not waived because this pass is clean.
