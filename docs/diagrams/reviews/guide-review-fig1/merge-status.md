# guide-review-fig1: blocked shared merge

Disposition: merge into `canonical-default-workflow`, not an independent redesign.
The shared target is outside plan-guide.json's exclusive ownership and has not been
promoted with collective input, applicable Build & Test, and coordinator-driven
revision/reassembly semantics. Neither legacy image is shown as current behavior
on the owned review/example pages.

The guide-local source, generated XML, PNG and hash are deliberately retained:
the plan forbids retiring them before the shared target and consumer promotion.
These are historical pending-merge assets, not newly validated surviving diagrams.
No four-pass manifest or current-correctness attestation is fabricated for them.

`docs/guide/review.md` now explains child assemble-ready, collective RAI / Build &
Test / Human Review, steering, tool approval projection and file review as prose.
`docs/guide/example-scenarios.md` uses the same accurate textual workflow.

The shared owner must promote canonical-default-workflow. The guide consumer can
then switch its image and source provenance, with collective workflow-dependent
alt text, and retire guide-review-fig1's source/PNG/hash while retaining this merge
history. No shared asset or inventory entry was edited by this execution.
