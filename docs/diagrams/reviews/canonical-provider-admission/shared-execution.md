# Shared diagram execution: owned artifacts validated; consumer handoffs blocked

> **USER VISUAL REJECTION - AUTHORING AND PROMOTION PAUSED (2026-09-13).**
> The earlier completion and style assessments below are superseded as visual
> acceptance. Wait for an explicitly approved shared calibration/template.
> No further public PNG/drawio/hash writes or automatic rollback. Research and
> mechanical evidence are preserved. Exact already-promoted paths: `visual-pause.md`
> and `visual-pause.json`.

Plan owner and authoring model: **shared / GPT-6 Astra**. No commits.
All 27 surviving/proposed canonical artifacts are promoted and validated.
End-to-end completion remains blocked only by the out-of-scope integrations below.
The completed coordinator pilot, product code, pipeline/skill code, global audit,
inventory, reconciliation and other shards were not edited by this shard.

## Disposition results

| Disposition | Planned identities | Result |
| --- | ---: | --- |
| Retain | 10 | 10 completed; stable PNG paths, editable A5 draw.io sources |
| Redesign | 16 | 16 completed; reviewed replacements at stable paths |
| Merge | 6 | 5 consolidated and retired; 1 held for foreign consumers |
| Remove | 4 | All 4 orphan source/generated/PNG/hash families removed |
| Reuse | 0 diagram identities | Concept-level reuse enacted on owned pages; foreign handoffs listed below |
| Proposed | 1 | `canonical-provider-admission` completed and published |

There are **27 current root manifests, 145 active pitch/pass artifact triples and
212 final arrow traces**. Each root manifest selects the accepted lineage, not a
superseded initial attempt. All selected pitch sources retain native notation and
the full Agentweaver Fluent visual hierarchy; every lineage has an inspected initial
PNG, at least four inspected post-pitch passes, pass-1 meaningful growth >=9x,
correction-only later passes and a final full arrow trace.

`shared-concept-status.json` records all **96 concept IDs: 89 completed and 7
blocked on external ownership**. Prose/table/inline concepts were kept in those
forms, not expanded into unauthorized diagram families.

### Consolidation and retirement

Five completed merges removed each obsolete JSON source, generated draw.io, PNG and hash:

| Retired family | Surviving canonical |
| --- | --- |
| `aks-block-diagram` | `canonical-aks-components` |
| `auth-security-fig2` | `auth-security-fig1` |
| `auth-security-fig3` | `auth-security-fig1` |
| `browser-console-fig1` | `assistant-runtime-fig1` |
| `git-integration-fig3` | `auth-security-fig1` |

Four removed orphan families are `a2a-bridge-fig2`, `events-observability-fig3`,
`landing-product-feature` and `token-usage-monitoring-fig1`.
These nine families account for 36 retired files. Also retired were the two owned
root AKS Excalidraw sources and the README-only duplicate
`docs/public/pitch-architecture.png`: **39 consolidation/removal files total**,
separate from the obsolete JSON/generated copies retired during surviving-family
draw.io migrations. README now reuses `docs/diagrams/email-architecture.png`.

Consumer scans found no remaining live consumers of these retired assets; historical
audit/review references are preserved as evidence. Ignored and external consumers
are not claimed to be known. Tombstones and replacement mappings live only in this plan.

## Research and ground-truth corrections

Exactly three separately launched bounded Astra research agents investigated
components/boundaries, directional flows and assurance/cleanup against implementation,
configuration, tests and current documentation. `research-thread-01.md` and
`research-thread-02.md` preserve their full returned outputs; `research-thread-03.md`
is explicitly an abridged preservation of the third returned result.
The separate retained-concept investigation is in `retained-concepts-evidence.json`
and its readable companion. Test-source citations are distinguished from executed tests.

Documentation and visuals now distinguish Entra user identity from optional GitHub
capabilities, current endpoint-bound authentication from historical selectors, and
OpenIddict broker validation from obsolete GitHub/raw-user/org-claim trust.
The normative no-repository-token sandbox contract is **not** represented as shipped:
configuration currently supplies repository material to AgentHost/tool options for
restricted direct commands, without blanket ambient shell credentials.

Other corrected contracts cover signed provider admission and live capability fences;
durable EF/Postgres cursor polling and per-run sequencing; all-inline promotion
fallback; private physical worktree dependencies; current harness verdict fields;
and workflow catalog, binder, authoring and selection behavior. Collective coordinator
assembly is not mislabeled as proven universal PR publication. No runtime changes
were used to resolve documentation conflicts.

## Changed paths

- The 27 owned `docs/diagrams/src/<name>.drawio` and stable PNG/hash families,
  their owned review directories, and three matching email-export PNG copies.
- The 39 consolidation/removal files above, plus obsolete surviving-family
  JSON/generated sources. Replay-tail assets are deliberately retained.
- `README.md`, `CONTRIBUTING.md`, `RELEASING.md`; owned architecture-decision,
  promotion/auth/two-App design and diagram README pages.
- Owned root docs covering catalog groupings, API/E2E/MCP/UI harnesses, index,
  MCP OAuth, durable events, tool approval, workflow binder/generation/library/
  selection; the authorized downloadable agent document.
- Only the shared plan's status/execution/tombstones and shared-owned review reports.

## Validation

- Explicit whole-README visual coverage: **2/2 audited**, including the HTML logo
  (retain) and Markdown architecture (reuse), plus the retired duplicate. Current
  implementation/configuration/test-source/docs evidence and the five-arrow trace
  are recorded in `readme-visual-coverage.md`. The machine report includes image
  hashes, dimensions, alt text and syntax; HTML images are no longer omitted.
  The follow-up standard docs build passed in **78.21 seconds**; see
  `readme-docs-build.log`. All 27 canonical drift checks passed again.
- Latest focused Node diagram tests: **27 passed**, `final-diagram-tests.log`.
- Pitch and iteration regressions: **5 + 14 passed**, `final-pitch-tests.log`
  and `final-iterate-tests.log`.
- Retained-contract focused Node tests: **35 passed**, `retained-focused-tests.log`.
  The attempted .NET no-build command produced no test execution evidence and is
  **not counted as a pass**; its empty log is preserved honestly.
- All 27 root manifests pass JSON Schema and the skill cross-field validator.
  All 145 active uncompressed XML/PNG/change-record triples exist.
  Page dimensions are 827x583 or 583x827 at pageScale=1; no invisible/off-page/
  duplicate/padding findings. Every pass-1 ratio passes
  `visible-semantic-canonical-xml-v1` >=9x.
- All 212 final arrow traces match XML endpoints. UML activation endpoints are
  checked against their named participant center; arrowless lifelines are excluded.
- All 27 selective render/drift checks pass. Canonical source equals the accepted
  final source; PNG pixels equal the inspected final PNG; source/image stamps
  identify pinned draw.io Desktop **31.4.5**. All three email copies match.
- All owned-page local image links resolve; all nine retired families and three
  non-catalog retired assets are absent. The three pre-existing contributor local
  document-link findings remain explicit: `.squad/templates/issue-lifecycle.md`,
  `.worktrees/`, `.squad/templates/worktree-reference.md`.
- Standard **`npm run docs:build` passed in 129.78 seconds** after final
  canonical consumer integration, in `final-docs-build.log`. Nonblocking Fluent
  directives, bundle-size and `promql` highlighting warnings remain.

`shared-validation.json` is the current machine report. Earlier blocked reports,
failed probes, superseded manifests and sparse pitches are preserved, not retroactively
certified. The 26 remediation results plus workflow-authoring's `a5-final/` lineage
resolve the former pitch/native-style/title-clipping/A5 blockers. The first representative
fully styled, inspected sample sent to the user was `canonical-workflow-authoring.png`.

## Exact residual work outside this shard's write scope

1. **One remaining merge:** owners of `docs/deep-dive/frontend.md` and
   `docs/deep-dive/orchestration.md` must replace `canonical-event-replay-tail`
   consumers with the durable canonical and correct channel/polling claims.
   Only then can this shard retire replay-tail source/generated/PNG/hash assets.
2. **Three shared memory/inbox concepts:** `docs/deep-dive/data-persistence.md`
   must own the compact shared memory schema/table; `docs/deep-dive/memory-decisions.md`
   must identify its inline ER as that model's focused view and link to the owner;
   `docs/experience/agent-communication.md` must map Proposed=pending,
   Finalized=merged plus linked decision, Rejected=rejected, and reuse the shared
   inbox model. No additional PNG is appropriate.
3. **Two provider-admission consumers:** `docs/deep-dive/api-core.md` and
   `docs/reference/api.md` must reuse the now-published `canonical-provider-admission`
   and describe execution-key acceptance, durable snapshot, pre-call fence and
   reprepare/retry semantics; display fingerprints are not execution authority.
4. **Two generated-agent concepts:** the authorized downloadable file is corrected,
   but `.github/agents/agentweaver.agent.md` and its mirrors
   `apps/Agentweaver.Api/Projects/Templates/agentweaver.agent.md` and
   `apps/Agentweaver.Web/wwwroot/agents/agentweaver.agent.md` are outside scope.
   Their owner must remove the retired code-review blueprint entry and synchronize
   the explicit outcome gate/manual sequence/shared journey link. The generator
   was not run across unauthorized paths.

Global inventory/reconciliation updates remain a coordinator-owned integration
handoff, not permission to change those files from this shard.
The SQL todo `revamp-diagrams-shared` is **blocked**, not done.
