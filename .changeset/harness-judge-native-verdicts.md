---
"agentweaver": minor
---

Enable MCP persona harness runs to export normalized evidence and a shared Judge prompt
for agent-native verdicts, instead of depending on a configured judge subprocess.
