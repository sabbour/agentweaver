---
"agentweaver": minor
---

Remove the unsupported `serial` workflow node type from authoring surfaces and reject legacy `serial` YAML with guidance to use ordinary workflow edges for sequential execution.
