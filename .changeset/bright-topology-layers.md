---
"agentweaver": minor
---

Add optional Kubernetes topology layers and bounded card-driven related-resource drilldowns so operators can inspect traffic, security, workload, storage, scaling, and Agentweaver infrastructure without cluttering the default runtime graph.
