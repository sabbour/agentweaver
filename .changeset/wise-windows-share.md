---
"agentweaver": patch
---

Unify project and platform-default GitHub Copilot OAuth callbacks onto one shared `/auth/github/copilot-app/callback` endpoint so GitHub App sign-in no longer depends on registering multiple callback URLs.
