---
"agentweaver": patch
---

Materialize the selected workflow YAML into each run worktree so agents can inspect custom workflows inside their sandbox without committing those files into repository history.
