---
"agentweaver": patch
---

Respect the remembered project on the assistant page so starting a session from `/assistant` works without an explicit `?project=` query string, and show a single actionable project-selection state when no project is available.
