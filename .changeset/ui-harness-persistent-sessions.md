---
"agentweaver": patch
---

Keep each UI harness scenario on one persistent browser page across separate CLI actions, with crash recovery, isolated concurrent sessions, and explicit cleanup on finish.
