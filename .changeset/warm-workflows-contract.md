---
"agentweaver": patch
---

Publish the supported workflow YAML grammar and the complete run-event query contract in OpenAPI so clients can construct valid workflows and event requests without hidden API knowledge.
