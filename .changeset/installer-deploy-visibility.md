---
"agentweaver": patch
---

Make Azure installer image work easier to diagnose with elapsed lifecycle progress, remove unused recording and worktree content from ACR build contexts, and accept the documented GitHub organization allowlist rules including the global `*` wildcard.
