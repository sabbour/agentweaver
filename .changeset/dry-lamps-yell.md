---
'agentweaver': patch
---

Contain the sidebar footer's alpha version badge so long dev build strings ellipsize inside the pill instead of spilling past it, while preserving enough room to keep short GitHub usernames visible.
