---
"agentweaver": patch
---

Align workflow node authoring across the API and visual editor: add Open pull request and Publish actions to the picker, preserve them through YAML edits, and enforce the shared node-type contract in frontend and server tests.
