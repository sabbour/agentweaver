---
"agentweaver": patch
---

Pin each run's resolved executable workflow definition with a content digest before execution so resume uses the workflow graph the run started with instead of today's project default.
