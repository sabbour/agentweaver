---
"agentweaver": patch
---

Simplify the Account settings MCP clients section to show only the MCP server URL, removing the per-client (Claude Desktop, VS Code, GitHub Copilot CLI) config snippets and copy buttons. Update the page description to cover both the MCP connection and the repository sandbox policy.
