---
"agentweaver": minor
---

Add a `custom` Azure installer image-source mode so deployments can import four operator-specified fully-qualified container image references instead of rebuilding in ACR or using only the repo-derived GHCR owner flow.
