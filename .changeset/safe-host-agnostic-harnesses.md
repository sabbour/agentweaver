---
"agentweaver": patch
---

Make API, UI, and MCP harnesses safe for arbitrary HTTPS hosts, confine credentials to configured origins, redact combined reports, and give deterministic MCP smoke tests ownership-aware project cleanup.
