---
'agentweaver': patch
---

Fix demo-recording approval-banner auto-approve watcher to catch all three approval-card UI surfaces (including ShellApprovalCard) and handle concurrent approval cards independently.
