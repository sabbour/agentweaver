---
"agentweaver": patch
---

Keep coordinator stories inline when task promotion would otherwise delegate the entire confirmed plan to backlog, so normal coordinator runs still dispatch live subtasks instead of completing with an empty delegated work plan.
