---
"agentweaver": patch
---

Make UI harness captures wait for the authenticated application shell and report persistent sign-in loading states as expired authentication instead of producing false-green evidence.
