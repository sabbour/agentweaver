---
"agentweaver": patch
---

Renamed ModelSource BYOK API output to `byok` while preserving legacy `microsoft-foundry` reads, and replaced the Project General tab provider picker with a deployment-level Platform settings note.
