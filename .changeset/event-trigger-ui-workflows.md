---
'agentweaver': minor
---

Add web UI editing for workflow event triggers with curated GitHub events, typed condition rows, and OR-within-a-field matching. The project webhook settings page now also shows an automatic webhook creation entry point with a clear coming-soon fallback alongside the existing manual setup steps.
