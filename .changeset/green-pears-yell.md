---
"agentweaver": patch
---

Tone down the GitHub connection-required banners in the repository connect dialogs so the expected "Connect GitHub" state shows as a warning-style prompt instead of a red error.
