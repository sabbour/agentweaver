---
"agentweaver": patch
---

Keep healthy in-progress coordinator runs and running child subtasks from being projected as failed when stale assembly-blocked context only means the coordinator is waiting for assembly eligibility.
