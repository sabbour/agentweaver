---
"agentweaver": patch
---

Prevent automatically selected document-only workflows from bypassing Build & Test when decomposition reveals code-producing work, while preserving explicit workflow overrides with a clear warning.
