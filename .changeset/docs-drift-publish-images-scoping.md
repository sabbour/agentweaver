---
"agentweaver": patch
---

Gate docs-drift workflow on relevant paths (was running 624 times/month on unrelated changes). Scope publish-images matrix to only build images whose sources changed (except on main/release).
