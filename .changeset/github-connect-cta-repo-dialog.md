---
"@agentweaver/web": patch
---

Show a "Connect GitHub" action in the Create Project from GitHub dialog when repositories fail to load because the GitHub Repo App isn't connected yet, instead of a raw error message with only a "Retry" option.
