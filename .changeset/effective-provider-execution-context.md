---
"agentweaver": patch
---

Expose a redacted effective model-provider context before generative AI actions and on active runs so interfaces can identify the project, platform, or personal provider without relying on the lossy persisted model source.
