---
"agentweaver": minor
---

Add opt-in Kubernetes topology layers with typed relationships, safe resource drill-down, and per-layer availability status while keeping the runtime view as the default.
