---
"agentweaver": patch
---

Allow the `/assistant` page to start platform-wide sessions without a selected project by using the platform-default GitHub Copilot binding on the backend, while still honoring an explicit `?project=` scope when one is provided.
