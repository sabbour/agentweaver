---
"agentweaver": patch
---

Fix the Sessions nav item rendering underlined/regular weight instead of matching other nav items (missing `aw-nav-item` class after the disclosure-to-Link conversion), and restore the expanded "New session" button to standard size to match other square icon buttons.
