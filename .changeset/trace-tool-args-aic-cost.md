---
"agentweaver": patch
---

Transaction traces now show tool call arguments and output in the Execute Tool detail panel, display AIC cost at every span level (turn, agent invocation, and run total), and redact sensitive values in tool payloads before persistence and API delivery.
