---
"agentweaver": patch
---

Make direct orchestrations with an explicit static fan workflow override execute the pinned authored topology instead of replacing it with a generic coordinator plan.
