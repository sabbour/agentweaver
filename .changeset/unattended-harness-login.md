---
"agentweaver": patch
---

The UI-harness Chrome login completes without a human. It clicks the Entra button, then waits for the cached SSO session to return. Use `--manual` for the previous Playwright Inspector flow.
