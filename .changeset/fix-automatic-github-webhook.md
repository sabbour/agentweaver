---
"agentweaver": patch
---

Make the project settings “Create webhook automatically” action provision or refresh the connected repository's signed GitHub webhook instead of returning a local placeholder error.
