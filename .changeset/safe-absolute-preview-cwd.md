---
"agentweaver": patch
---

Allow supervised preview processes to use canonical absolute working directories when they resolve to the run worktree or one of its subdirectories, while continuing to reject sandbox escapes.
