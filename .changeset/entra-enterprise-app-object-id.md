---
"agentweaver": patch
---

Add an optional Entra enterprise application object ID setting so Account settings can deep-link directly to the Azure Portal users-and-groups blade when that deployment-specific object ID is configured.
