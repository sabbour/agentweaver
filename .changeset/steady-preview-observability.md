---
"agentweaver": patch
---

Keep approved managed previews alive while public routing converges, let cluster resource details
use the available page width, give transaction traces a separately configurable query budget, and
make deployment provenance checks use bounded exact-tag registry lookups.
