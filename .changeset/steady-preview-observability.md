---
"agentweaver": patch
---

Keep approved managed previews alive while public routing converges, let cluster resource details
use the available page width, and give transaction traces a separately configurable query budget.
