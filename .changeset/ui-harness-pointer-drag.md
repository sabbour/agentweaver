---
"agentweaver": minor
---

Add a UI-harness pointer drag command with stable workflow-editor node and handle targets, safe element-relative coordinates, configurable movement steps, and failure evidence so canvas repositioning and drag-to-connect regressions can be reproduced.
