"agentweaver": patch
---

Restore GitHub connection callbacks to the page that started them, and reopen GitHub project flows after a successful Repo App return so repository access is recognized immediately.
