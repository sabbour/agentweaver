---
"agentweaver": patch
---

Fixed a build break on Settings page introduced by a merge that dropped the `getAuthConfig` API client method and the `formActions` style, both used by the Entra ID admin link and GitHub Repo App connect button.
