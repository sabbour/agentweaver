---
"agentweaver": patch
---

`azure:provision-infra` interactive installer now supports arrow-key selection (with the numbered prompt as a fallback when raw-mode TTY is unavailable) and walks you through creating a GitHub OAuth App (with link and callback-URL guidance) before asking for the client ID/secret.
