---
"agentweaver": patch
---

Require explicitly independent generation requests with exact, disjoint content outputs to produce a validated fan region instead of silently returning a sequential workflow.
