---
"agentweaver": minor
---

Add structured GitHub event-trigger predicates for workflows, including label, branch, review-state, ref, category, and fixed-regex comment matching, plus REST trigger-config endpoints for UI-driven editing.
