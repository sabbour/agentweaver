---
"agentweaver": patch
---

Keep the Create-from-GitHub dialog open when toggling the linked-account filter, and default the repository list to the currently selected GitHub account (including the gallery account switcher) instead of silently clearing the filter after repos load.
