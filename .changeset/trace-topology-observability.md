---
"agentweaver": patch
---

Use the available page width for cluster resource details and give transaction traces a dedicated,
configurable query budget with bounded retries so cold telemetry loads no longer fail prematurely.
