---
"agentweaver": patch
---

Fix switching a project between linked GitHub accounts so the selected identity remains active for request and background operations instead of reverting to the default account.
