---
"agentweaver": patch
---

Allow `release:publish` to run from a normal built checkout containing standard dependency, build, test, and harness outputs, while continuing to reject untracked source and unexpected ignored files.
