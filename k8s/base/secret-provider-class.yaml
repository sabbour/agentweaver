apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: agentweaver-secrets
  namespace: agentweaver
  annotations:
    secrets-store.csi.k8s.io/rotation-poll-interval: "2m"
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "false"
    clientID: "changeme"
    keyvaultName: "changeme"
    tenantId: "changeme"
    objects: |
      array:
        - |
          objectName: mcp-api-key
          objectType: secret
        - |
          objectName: appinsights-connection-string
          objectType: secret
        - |
          objectName: copilot-app-client-id
          objectType: secret
        - |
          objectName: copilot-app-client-secret
          objectType: secret
        - |
          objectName: repo-app-client-id
          objectType: secret
        - |
          objectName: repo-app-client-secret
          objectType: secret
        - |
          objectName: repo-app-id
          objectType: secret
  secretObjects:
    - secretName: agentweaver-secrets
      type: Opaque
      data:
        - objectName: mcp-api-key
          key: mcp-api-key
        - objectName: appinsights-connection-string
          key: appinsights-connection-string
        - objectName: copilot-app-client-id
          key: copilot-app-client-id
        - objectName: copilot-app-client-secret
          key: copilot-app-client-secret
        - objectName: repo-app-client-id
          key: repo-app-client-id
        - objectName: repo-app-client-secret
          key: repo-app-client-secret
        - objectName: repo-app-id
          key: repo-app-id
---
# Base AgentHost token SPC. Keep this committed manifest installation-only: the API creates a
# per-run SecretProviderClass for each AgentHost pod containing only that run owner's
# ghtok-user--{base32(userId)} entry, then points a run-scoped SandboxTemplate at it.
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: agentweaver-user-tokens
  namespace: agentweaver
  annotations:
    secrets-store.csi.k8s.io/rotation-poll-interval: "2m"
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "false"
    clientID: "changeme"
    keyvaultName: "changeme"
    tenantId: "changeme"
    objects: |
      array:
        - |
          objectName: ghtok-installation
          objectType: secret
  secretObjects:
    - secretName: agentweaver-user-tokens
      type: Opaque
      data:
        - objectName: ghtok-installation
          key: installation.json
