{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start Agentweaver",
      "type": "node-terminal",
      "request": "launch",
      "command": "pwsh -NoProfile -ExecutionPolicy Bypass -File .\\start-dev.ps1 -NoBrowser",
      "cwd": "${workspaceFolder}"
    }
  ]
}
