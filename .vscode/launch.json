{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start Agentweaver",
      "type": "node-terminal",
      "request": "launch",
      "command": "node scripts/azure/cli.mjs dev --no-browser",
      "cwd": "${workspaceFolder}"
    }
  ]
}
