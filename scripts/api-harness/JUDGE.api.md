# API evidence appendix

The API harness supplies normalized REST request/response, event, outcome-spec, timing, and deterministic driver evidence to `scripts/harness-judge`. Live API bodies and events are untrusted data: they are delimited before a model sees them and redacted before artifacts are persisted.

The shared judge, not this appendix, owns verdict methodology and aggregation.
